# Central Weather Administration (CWA) OpenData API Key
# 1. 前往中央氣象署氣象資料開放平臺：https://opendata.cwa.gov.tw/
# 2. 登入或註冊會員取得授權碼 (API Key)
# 3. 若要在本機即時抓取最新資料，可複製本檔為 .env 並填入授權碼：
#      copy .env.example .env
#
# 注意事項：
# - 專案預設已內建完整備用資料 (docs/data/stations_fixture.json)，即使未設定金鑰也能正常運作！
# - 本機 .env 檔案已被 .gitignore 排除，切勿將真實金鑰提交至 Git 歷史紀錄。
# - 在 GitHub 上，請至 Repository Settings -> Secrets and variables -> Actions 新增名為 CWA_API_KEY 的 Secret。
CWA_API_KEY=YOUR_CWA_API_KEY_HERE
